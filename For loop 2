read a number and print number is prime or not divisible ny i and itself.
#include <stdio.h>

int main()
{
    int i,n,c=0;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
    if(n%i==0)
    c++;
    }
    if(c==2)
    printf("Prime no");
    else
    printf("Not Prime no");
    
    return 0;  
}
