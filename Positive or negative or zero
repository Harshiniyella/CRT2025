#include <stdio.h>
#include <math.h>
int main()
{
    int a;
    scanf("%d",&a);
    if(a>0)
    {
        printf("it is positive number");
    }
    else
    if(a<0)
    {
        printf("it is negative number");
    }
    else
    {
        printf("it is zero");
    }
return 0;
