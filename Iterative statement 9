Read a number and printf number is palindrome no or not.
#include <stdio.h>

int main()
{
    int n,d,s=0,t;
    scanf("%d",&n);
    while(n>0)
    {
        d=n%10;
        n=n/10;
        s=s+d*d*d;
    }
    if(s==t)
    printf("Palindrome no");
    else
    printf("Not Palindrome no");
    
    return 0;  
}
