Read three digits number and print number is armstrong or not.
#include <stdio.h>

int main()
{
    int n,d,s=0,t;
    scanf("%d",&n);
    while(n>0)
    {
        d=n%10;
        n=n/10;
        s=s+d*d*d;
    }
    if(s==t)
    printf("Armstrong no");
    else
    printf("Not Armstrong no");
    
    return 0;  
}
